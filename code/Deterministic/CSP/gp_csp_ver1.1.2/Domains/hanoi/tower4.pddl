(define (problem tower4)
	(:domain hanoi)
	(:objects small medium large huge pega pegb pegc)
	(:init (on small medium)
	       (on medium large)
   	       (on large huge)
	       (on huge pega)
	       (top small)
 	       (top pegb)
	       (top pegc)
	       (smaller small medium)
	       (smaller small large)
	       (smaller small huge)
	       (smaller small pega)  
	       (smaller small pegb) 
	       (smaller small pegc)
	       (smaller medium large) 
	       (smaller medium huge)
	       (smaller medium pega) 
	       (smaller medium pegb) 
	       (smaller medium pegc) 
	       (smaller large huge)
	       (smaller large pega) 
	       (smaller large pegb) 
	       (smaller large pegc)
	       (smaller huge pega) 
	       (smaller huge pegb) 
	       (smaller huge pegc)) 
	(:goal (and (on small medium)
 		    (on medium large)
		    (on large huge)
		    (on huge pegc))))
