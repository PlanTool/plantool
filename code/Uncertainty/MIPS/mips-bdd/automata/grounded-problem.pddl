(define (problem grounded-pathways-04)
(:domain grounded-pathways-propositional)

  (:init 
     (num-subs.l0)
  )
  (:goal 
    (and 
     (goal1)
     (goal2)
     (goal3)
     (goal4)
     )
  )
)
